{
  "include": ["src"], // src 폴더 안에 있는 모든 파일을 JS로 컴파일하라고 지정함..
  "compilerOptions": {
    // 컴파일 옵션 지정
    "outDir": "build", // 컴파일된 JS를 어디의 폴더에 넣을 것인지 지정. build라는 폴더에 넣기로 함.
    "target": "ES6", // 컴파일된 JS를 ES6코드로 구성하는 옵션.
    "lib": ["ES6", "DOM"], // 컴파일되어 나온 JS코드가 어떤 환경에서 작동하는지 알려주는 작업이다.
    // ES6를 지원하는 서버에서 작동한다는 것을 알려주기위해 "ES6"를 적어놓았고,
    // 브라우저에서 작동하는 것을 알려주기 위해 DOM을 적어 놓았다.
    // lib.dom.d.ts에서 해당 라이브러리를 읽을 수 있도록 활성화 한 것이다.
    "strict": true // JS파일을 TS에 import 했을 때 자동완성 및 오류출력을 위해 사용하는 모드.
  }
}
